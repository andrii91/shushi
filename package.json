{
  "name": "www",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vue-tsc -b && vite build",
    "preview": "vite preview",
    "serve": "PHP_CLI_SERVER_WORKERS=4 php -S localhost:8000 -t dist",
    "start": "npm run build && PHP_CLI_SERVER_WORKERS=4 php -S localhost:8000 -t dist"
  },
  "dependencies": {
    "@vueuse/head": "^2.0.0",
    "sass": "^1.83.4",
    "vue": "^3.5.13",
    "vue-i18n": "^10.0.6"
  },
  "devDependencies": {
    "@types/node": "^25.9.2",
    "@vitejs/plugin-vue": "^5.2.1",
    "@vue/tsconfig": "^0.7.0",
    "scss-reset": "^1.4.6",
    "typescript": "~5.7.2",
    "vite": "^6.2.0",
    "vue-tsc": "^2.2.4"
  }
}
